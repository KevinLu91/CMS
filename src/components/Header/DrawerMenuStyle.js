import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  box: {
    display: "flex",
    marginTop: 15,
    marginBottom: 15,
    cursor: "pointer",
  },
  icon: {
    marginRight: 15,
  },
}));
